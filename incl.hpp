#include <iostream>

#include <tuple>

#include <list>

#include <string>

#include <filesystem>

#include <fstream>

using std::cin;
using std::cout;
using std::string;
using std::tuple;
using std::endl;
using std::get;
using std::list;
using std::system;
using std::getline;
using std::fstream;
using std::ios;
